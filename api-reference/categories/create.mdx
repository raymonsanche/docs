---
title: "Create category"
api: "POST /categories"
description: "Create a new category"
---

Create a new product category.

## Endpoint

```
POST /categories
```

## Request body

<ParamField body="name" type="string" required>
  Category name
</ParamField>

<ParamField body="description" type="string">
  Category description
</ParamField>

## Request example

```bash
curl -X POST http://localhost:8000/categories \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Electronics",
    "description": "Electronic devices and accessories"
  }'
```

## Response

<ResponseField name="id" type="integer">
  Unique identifier for the newly created category (auto-generated)
</ResponseField>

<ResponseField name="name" type="string">
  Category name
</ResponseField>

<ResponseField name="description" type="string">
  Category description
</ResponseField>

### Success response (201)

```json
{
  "id": 1,
  "name": "Electronics",
  "description": "Electronic devices and accessories"
}
```

## Use cases

- Organize products into logical groupings
- Create a taxonomy for your product catalog
- Set up category hierarchies for navigation

<Note>
The ID field is automatically assigned by the server. Do not include it in your request.
</Note>
