---
title: "Create product"
api: "POST /products"
description: "Create a new product"
---

Create a new product in the inventory.

## Endpoint

```
POST /products
```

## Request body

<ParamField body="name" type="string" required>
  Product name
</ParamField>

<ParamField body="price" type="number">
  Product price
</ParamField>

<ParamField body="stock" type="integer">
  Quantity in stock
</ParamField>

<ParamField body="description" type="string">
  Product description (optional)
</ParamField>

## Request example

```bash
curl -X POST http://localhost:8000/products \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Laptop",
    "price": 999.99,
    "stock": 50,
    "description": "High-performance laptop"
  }'
```

## Response

<ResponseField name="id" type="integer">
  Unique identifier for the newly created product (auto-generated)
</ResponseField>

<ResponseField name="name" type="string">
  Product name
</ResponseField>

<ResponseField name="price" type="number">
  Product price
</ResponseField>

<ResponseField name="stock" type="integer">
  Quantity in stock
</ResponseField>

<ResponseField name="description" type="string">
  Product description
</ResponseField>

### Success response (201)

```json
{
  "id": 1,
  "name": "Laptop",
  "price": 999.99,
  "stock": 50,
  "description": "High-performance laptop"
}
```

## Rules

- Product ID is auto-generated
- Name is required
- All provided fields are stored

## Use cases

- Add a new item to your product catalog
- Register inventory for a new product line
- Create products to associate with categories

<Note>
The ID field is automatically assigned by the server. Do not include it in your request.
</Note>
